<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uw.tcss450.aboutme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TxtButtonActivity" />
        <activity android:name=".ImgButtonActivity" />
        <activity android:name=".WebButtonActivity" />
        <activity android:name=".ToastButtonActivity" />
        <activity android:name=".DialogButtonActivity" />
        <activity android:name=".EthansActivity"></activity>
    </application>

</manifest>